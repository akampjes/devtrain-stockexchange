<%= simple_form_for(@order) do |f| %>
  <% if @order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% @order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.association :stock %>
  </div>
  <div class="field">
    <%= f.input :kind, collection: [:buy, :sell] %>
  </div>
  <div class="field">
    <%= f.input :quantity %>
  </div>
  <div class="field">
    <%= f.input :price %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
